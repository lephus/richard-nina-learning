# Lát 2c — Ôn tập nhóm: Kế hoạch triển khai

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ô "Ôn tập" của mỗi nhóm dẫn tới bài thi 60 câu phủ hết 60 từ của nhóm, và chữ "sắp có" cuối cùng biến mất khỏi màn hình từ vựng.

**Architecture:** Không thêm route, không thêm bảng, không đổi bộ máy bài thi. Một Server Action mới dựng bài `type: "review"` với `scope = [buổi₁, buổi₂]`, cộng một bản sửa cho `batDauBoTuc` để nó xử lý được `scope` nhiều hơn một phần tử.

**Tech Stack:** Next.js 16 App Router (Server Actions), React 19, Supabase, Vitest, Playwright.

**Spec:** `docs/superpowers/specs/2026-08-13-lat-2c-on-tap-nhom-design.md`

## Global Constraints

- Tiếng Việt cho mọi chữ người dùng thấy và mọi chú thích; **đủ dấu** trong `src/` và `tests/`, **không dấu** trong `scripts/`, `supabase/migrations/` và thông điệp commit. Chú thích giải thích **vì sao**.
- Bài ôn tập ghi `type: "review"` và **`scope = lessonsOf(group)`** — mảng hai ordinal buổi, đúng thứ tự hàm đó trả về. `progress.ts:108` so khớp bằng `sameScope`; sai thứ tự thì ô Ôn tập vĩnh viễn hiện "chưa làm" mà không có lỗi nào bật ra.
- Bảng `blankAnswers` phải **gộp từ cả hai buổi** — `blank_answers_for_lesson` chỉ nhận một `lesson_id`.
- Nguồn nhiễu của bài ôn tập là **cả 60 từ**.
- Giữ 60 câu. Không hạ xuống 30 ở lát này.
- `PASS_MARK` vẫn là một hằng số 80% cho mọi loại — không thêm bảng tra theo loại.
- Không có ESLint; không thêm `eslint-disable`. `params` là `Promise` (Next 16).
- Không chạy `supabase db push`, `supabase link`, hay `psql`. Lát này **không cần** migration nào.

## Cấu trúc tệp

| Tệp | Trách nhiệm |
|---|---|
| `src/lib/exam/load-scope.ts` | **Tạo.** Gom từ + bảng đáp án cho một dải buổi. Dùng chung cho cả ba đường dựng bài. |
| `tests/exam-load-scope.test.ts` | **Tạo.** |
| `src/app/(app)/exam/[id]/actions.ts` | **Sửa.** Thêm `batDauOnTap`; dùng lại helper mới. |
| `src/app/(app)/exam/[id]/ket-qua/actions.ts` | **Sửa.** `batDauBoTuc` xử lý `scope` nhiều phần tử; dùng lại helper mới. |
| `src/app/(app)/vocab/(list)/page.tsx` | **Sửa.** Ô Ôn tập gọi Server Action; bỏ nhánh `"sắp có"`. |
| `src/components/vocab/*` hoặc chỗ dựng ô hoạt động | **Sửa.** Ô Ôn tập nhận action thay vì chỉ nhãn chết. |
| `e2e/on-tap.spec.ts` | **Tạo.** |

---

### Task 1: Gom từ theo dải buổi (`src/lib/exam/load-scope.ts`)

Ba đường dựng bài (buổi, ôn tập, bổ túc) đều cần đúng một việc: từ một dải ordinal buổi, lấy ra `VocabLite[]` và `Map<wordId, blankAnswer>`. Hiện logic đó đã bị chép hai bản gần như y hệt trong `exam/[id]/actions.ts` và `ket-qua/actions.ts` — vòng soát cuối lát 2b đã ghi lại như một điểm nợ. Thêm bản thứ ba là lúc phải gộp.

**Files:**
- Create: `src/lib/exam/load-scope.ts`
- Test: `tests/exam-load-scope.test.ts`

**Interfaces:**
- Consumes: `VocabLite` từ `@/lib/vocab/word`.
- Produces:
  ```ts
  export interface PhamViBaiThi {
    words: VocabLite[];
    blankAnswers: Map<number, string>;
  }
  export async function napPhamVi(
    supabase: SupabaseClient,
    lessonOrdinals: readonly number[],
  ): Promise<PhamViBaiThi>;
  ```

- [ ] **Step 1: Viết test thất bại**

Tạo `tests/exam-load-scope.test.ts`, theo khuôn `describe.skipIf(!hasEnv)` của `tests/db-integrity.test.ts`, dùng client service role:

```ts
import { createClient } from "@supabase/supabase-js";
import { describe, expect, it } from "vitest";
import { napPhamVi } from "@/lib/exam/load-scope";

const URL = process.env.SUPABASE_URL;
const SERVICE = process.env.SUPABASE_SERVICE_ROLE_KEY;
const hasEnv = Boolean(URL && SERVICE);

describe.skipIf(!hasEnv)("napPhamVi", () => {
  const db = createClient(URL ?? "http://localhost", SERVICE ?? "x", {
    auth: { persistSession: false },
  });

  it("một buổi cho 30 từ và đủ 30 đáp án điền", async () => {
    const { words, blankAnswers } = await napPhamVi(db, [1]);
    expect(words).toHaveLength(30);
    expect(blankAnswers.size).toBe(30);
    for (const w of words) expect(blankAnswers.has(w.id)).toBe(true);
  });

  it("hai buổi cho 60 từ, không trùng, và đủ 60 đáp án — đây là bài ôn tập nhóm", async () => {
    const { words, blankAnswers } = await napPhamVi(db, [1, 2]);
    expect(words).toHaveLength(60);
    expect(new Set(words.map((w) => w.id)).size).toBe(60);
    expect(blankAnswers.size).toBe(60);
    // Gộp thiếu một nửa là lỗi âm thầm: buildVocabExam sẽ ném ở câu điền đầu
    // tiên thuộc buổi bị thiếu, và người học chỉ thấy trang lỗi.
    for (const w of words) expect(blankAnswers.has(w.id)).toBe(true);
  });

  it("giữ đúng thứ tự buổi rồi tới thứ tự từ trong buổi", async () => {
    const { words } = await napPhamVi(db, [1, 2]);
    const buoi1 = await napPhamVi(db, [1]);
    expect(words.slice(0, 30).map((w) => w.id)).toEqual(buoi1.words.map((w) => w.id));
  });

  it("ném khi một ordinal không tồn tại, thay vì trả phạm vi ngắn hơn", async () => {
    await expect(napPhamVi(db, [999])).rejects.toBeTruthy();
  });
});
```

- [ ] **Step 2: Chạy test để chắc chắn nó đỏ**

Run: `npm test -- exam-load-scope`
Expected: FAIL — không giải được import `@/lib/exam/load-scope`.

- [ ] **Step 3: Viết `src/lib/exam/load-scope.ts`**

Đọc `src/app/(app)/exam/[id]/actions.ts` và `src/app/(app)/exam/[id]/ket-qua/actions.ts` trước — hai bản chép hiện có là nguồn của hàm này. Gom lại, giữ nguyên hai điểm đã trả giá trong đó:

- Chuẩn hoá quan hệ nhúng: `postgrest-js` đôi khi trả quan hệ 1-1 thành **mảng**; không chuẩn hoá thì ô render rỗng mà không có lỗi nào.
- `blank_answers_for_lesson` trả **JSONB object**, không phải mảng — dùng `Object.entries`.

Hàm nhận **ordinal buổi**, không phải `lessons.id`, và tự tra id từ ordinal. Ném khi thiếu — không bao giờ trả phạm vi ngắn hơn yêu cầu, vì `buildVocabExam` phía sau sẽ ném ở một chỗ khó truy hơn nhiều.

- [ ] **Step 4: Chạy test để xác nhận xanh**

Run: `npm test -- exam-load-scope`
Expected: PASS cả 4 test.

- [ ] **Step 5: Commit**

```bash
git add src/lib/exam/load-scope.ts tests/exam-load-scope.test.ts
git commit -m "feat(2c): gom tu theo dai buoi, dung chung cho ca ba duong dung bai"
```

---

### Task 2: Dựng bài ôn tập và sửa bổ túc cho phạm vi nhiều buổi

**Files:**
- Modify: `src/app/(app)/exam/[id]/actions.ts` (thêm `batDauOnTap`, dùng `napPhamVi`)
- Modify: `src/app/(app)/exam/[id]/ket-qua/actions.ts` (`batDauBoTuc` xử lý `scope` nhiều phần tử, dùng `napPhamVi`)
- Test: `tests/exam-review.test.ts`

**Interfaces:**
- Consumes: `napPhamVi` (Task 1); `createVocabExam`, `timHoacDungBaiThi` từ `@/lib/exam/run`; `lessonsOf` từ `@/lib/curriculum/groups`.
- Produces: Server Action `batDauOnTap(groupId: number): Promise<void>`.

- [ ] **Step 1: Viết test thất bại**

Tạo `tests/exam-review.test.ts`, khuôn tạo người dùng thật như `tests/exam-security.test.ts`:

```ts
it("bài ôn tập ghi scope là hai ordinal buổi, đúng thứ tự lessonsOf", async () => {
  const { words, blankAnswers } = await napPhamVi(admin, lessonsOf(1));
  const id = await createVocabExam(
    alice, aliceId, "review", [...lessonsOf(1)], words, blankAnswers, 1,
  );
  const { data } = await admin
    .from("assessments").select("type, scope").eq("id", id).single();
  expect(data?.type).toBe("review");
  // progress.ts so khớp bằng sameScope; sai thứ tự thì ô Ôn tập vĩnh viễn
  // hiện "chưa làm" mà không có lỗi nào bật ra.
  expect(data?.scope).toEqual([1, 2]);
});

it("bài ôn tập có đúng 60 câu, mỗi từ một câu", async () => {
  const { words, blankAnswers } = await napPhamVi(admin, lessonsOf(1));
  const id = await createVocabExam(
    alice, aliceId, "review", [...lessonsOf(1)], words, blankAnswers, 2,
  );
  const { data } = await admin
    .from("assessment_items").select("ref_id").eq("assessment_id", id);
  expect(data).toHaveLength(60);
  expect(new Set((data ?? []).map((r) => r.ref_id)).size).toBe(60);
});
```

Thêm một test cho bổ túc của bài ôn tập: dựng bài `review`, trả lời sai một từ **thuộc buổi thứ hai**, nộp, rồi dựng bổ túc — khẳng định bổ túc dựng được và chứa đúng từ đó. Trước bản sửa, `batDauBoTuc` chỉ đọc `scope[0]` nên từ của buổi thứ hai không nằm trong phạm vi nạp và bài bổ túc sẽ ném hoặc thiếu từ.

- [ ] **Step 2: Chạy test để chắc chắn nó đỏ**

Run: `npm test -- exam-review`
Expected: FAIL — `napPhamVi` chưa được dùng ở actions, và test bổ túc đỏ vì `scope[0]`.

- [ ] **Step 3: Viết `batDauOnTap`**

Trong `src/app/(app)/exam/[id]/actions.ts`, thêm Server Action nhận `groupId`, quy ra `lessonsOf(groupId)`, nạp phạm vi bằng `napPhamVi`, rồi gọi đúng đường mà `batDauBaiThi` đang dùng (`timHoacDungBaiThi`), với `type: "review"` và `scope` là hai ordinal.

Ghi chú tại chỗ vì sao `scope` là hai ordinal chứ không phải `[groupId]`.

- [ ] **Step 4: Sửa `batDauBoTuc` cho phạm vi nhiều buổi**

Trong `ket-qua/actions.ts`, thay `const lessonId = (cha.scope as number[])[0]` bằng việc nạp **toàn bộ** `cha.scope` qua `napPhamVi`. Sửa cả chú thích ở trên nó — chú thích hiện tại khẳng định "bài lesson/remedial luôn ghi đúng một phần tử", và sau lát này khẳng định đó không còn đúng cho bài `review`.

Nguồn nhiễu của bài bổ túc vẫn là toàn bộ phạm vi bài cha — giờ là 60 từ thay vì 30 khi cha là bài ôn tập.

- [ ] **Step 5: Chạy test để xác nhận xanh**

Run: `npm test -- exam-review`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add "src/app/(app)/exam/[id]/actions.ts" "src/app/(app)/exam/[id]/ket-qua/actions.ts" tests/exam-review.test.ts
git commit -m "feat(2c): dung bai on tap nhom va sua bo tuc cho pham vi nhieu buoi"
```

---

### Task 3: Nối ô Ôn tập và bỏ chữ "sắp có"

**Files:**
- Modify: `src/app/(app)/vocab/(list)/page.tsx`
- Modify: component dựng ô hoạt động (đọc `(list)/page.tsx` để tìm đúng tên)
- Test: `e2e/on-tap.spec.ts`

**Interfaces:**
- Consumes: `batDauOnTap` (Task 2).

- [ ] **Step 1: Viết e2e thất bại**

Tạo `e2e/on-tap.spec.ts` với `login()` cục bộ theo đúng quy ước từng tệp e2e của dự án:

```ts
test("ô Ôn tập không còn ghi sắp có", async ({ page }) => {
  await login(page);
  await page.goto("/vocab");
  await expect(page.getByText("sắp có")).toHaveCount(0);
});

test("bấm ô Ôn tập vào được bài thi 60 câu", async ({ page }) => {
  await login(page);
  await page.goto("/vocab");
  await page.getByTestId("on-tap-1").click();

  await expect(page).toHaveURL(/\/exam\/\d+$/);
  await expect(page.getByTestId("exam-tien-do")).toHaveText("Câu 1/60");
  await expect(page.getByTestId("exam-option")).toHaveCount(4);
});
```

Dọn bài đang làm dở giữa các test như `e2e/exam.spec.ts` đang làm — chỉ số `assessments_one_in_progress` chỉ cho một bài mỗi người, và bộ e2e chạy `workers: 1` trên một tài khoản dùng chung.

- [ ] **Step 2: Chạy e2e để chắc chắn nó đỏ**

Run: `npm run test:e2e -- on-tap`
Expected: FAIL — ô Ôn tập vẫn là chữ chết, `on-tap-1` chưa tồn tại.

- [ ] **Step 3: Nối ô Ôn tập**

Đọc `src/app/(app)/vocab/(list)/page.tsx` quanh dòng 92 để tìm component dựng ô. Ô Ôn tập (`i === 2`) phải render một `<form action={batDauOnTap.bind(null, group)}>` với nút mang `data-testid={"on-tap-" + group}`, cùng khuôn nút LÀM BÀI của `deck.tsx`.

Hai ô buổi thường giữ nguyên là `<Link>` — chúng dẫn tới trang học, không dựng bài.

- [ ] **Step 4: Bỏ nhánh "sắp có"**

Trong `describe()` của cùng tệp, bỏ `return isReview ? "sắp có" : "chưa học";` còn `return "chưa học";`, và xoá khối chú thích giải thích vì sao ô Ôn tập từng là "sắp có" — nó không còn đúng. Nếu `isReview` không còn chỗ dùng nào khác thì bỏ luôn tham số, để `tsc` chỉ ra mọi nơi gọi.

- [ ] **Step 5: Chạy e2e để xác nhận xanh**

Run: `npm run test:e2e -- on-tap`
Expected: PASS cả 2 test.

- [ ] **Step 6: Chạy toàn bộ**

Run: `npm test && npx tsc --noEmit && npm run build && npm run test:e2e -- exam`
Expected: tất cả xanh. Bộ `exam` phải chạy lại vì Task 2 đã đụng vào `batDauBoTuc`.

- [ ] **Step 7: Commit**

```bash
git add -A
git commit -m "feat(2c): noi o On tap voi bai thi 60 cau, bo chu sap co cuoi cung"
```

---

## Đối chiếu với spec

| Mục spec | Task |
|---|---|
| 2. `scope` là hai ordinal buổi, đúng thứ tự | 2 |
| 3. Gộp bảng đáp án hai buổi; nguồn nhiễu cả 60 từ | 1, 2 |
| 4. Ô Ôn tập gọi Server Action; bỏ "sắp có" | 3 |
| 4. Bổ túc của bài `review` xử lý `scope` hai phần tử | 2 |
| 5. Không phá đường "một bài đang làm dở" | 2 (dùng `timHoacDungBaiThi`) |
| 6. Kiểm thử unit / tích hợp / e2e | 1, 2, 3 |
